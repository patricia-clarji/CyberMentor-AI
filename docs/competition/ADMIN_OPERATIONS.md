# CMS Administration

Content administrators use server-side permissions for review assignment, publication, rollback, media, flags, audit access, and due-job processing. Dashboard values are database-derived; worker health is deliberately reported as unknown when no continuous worker is configured.
