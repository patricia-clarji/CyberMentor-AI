# Media Library

Media uploads use an allowlist, size limit, sanitized storage names, path-traversal protection, metadata tracking, and usage records. Deletion is soft and blocked when media is referenced by a published version.
